import{CheckCircle2Icon,LoaderCircleIcon}from"lucide-react"
import{AlertDialog,AlertDialogAction,AlertDialogCancel,AlertDialogContent,AlertDialogDescription,AlertDialogFooter,AlertDialogHeader,AlertDialogTitle,AlertDialogTrigger}from"@/components/ui/alert-dialog"
import{Badge}from"@/components/ui/badge"
import{Button}from"@/components/ui/button"
import{Card,CardContent,CardFooter,CardHeader,CardTitle}from"@/components/ui/card"
import type{RecoveryPlan}from"@/lib/types"
export function RecoveryPanel({plans,isApproving,onApprove}:{plans:RecoveryPlan[];isApproving:boolean;onApprove:(id:string)=>Promise<void>}){return <aside className="recovery-rail"><header><div><h2>Recovery plans</h2><p>Ranked by estimated recovery time</p></div><Badge variant="secondary">{plans.length}</Badge></header>{plans.length===0?<Card className="empty-plan"><CardContent><CheckCircle2Icon/><strong>No recovery required</strong><span>The production is operating normally.</span></CardContent></Card>:plans.map((plan,index)=><Card key={plan.plan_id} className="plan-card" data-recommended={plan.recommended}><CardHeader><span className="plan-rank">{index+1}</span><CardTitle>{plan.title}</CardTitle>{plan.recommended?<Badge>Recommended</Badge>:null}</CardHeader><CardContent><dl><div><dt>Recovery time</dt><dd>{plan.estimated_recovery_minutes} min</dd></div><div><dt>Added cost</dt><dd>{plan.estimated_added_cost_usd>=0?"+":""}${plan.estimated_added_cost_usd}</dd></div><div><dt>Risk</dt><dd>{plan.risk}</dd></div></dl></CardContent>{plan.recommended?<CardFooter><AlertDialog><AlertDialogTrigger render={<Button className="approve-button" disabled={isApproving}/>}>{isApproving?<LoaderCircleIcon className="animate-spin" data-icon="inline-start"/>:<CheckCircle2Icon data-icon="inline-start"/>}Approve plan</AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Approve {plan.title}?</AlertDialogTitle><AlertDialogDescription>This starts recovery immediately and records the operator approval in the audit log. Estimated recovery: {plan.estimated_recovery_minutes} minutes. Added cost: ${plan.estimated_added_cost_usd}.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={()=>void onApprove(plan.plan_id)}>Approve and execute</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></CardFooter>:null}</Card>)}</aside>}

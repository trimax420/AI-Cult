[
  {
    "title": "Project Nova GPU memory exhaustion",
    "expression": "render_gpu_memory_utilization_percent > 95",
    "for": "20s",
    "severity": "critical",
    "component": "gpu",
    "summary": "GPU memory is critically high",
    "description": "GPU memory utilization exceeded 95%, indicating likely CUDA OOM risk."
  },
  {
    "title": "Project Nova trailer throughput below required rate",
    "expression": "render_current_throughput_per_hour < render_required_throughput_per_hour and deriv(render_queue_critical_depth[30s]) > 0",
    "for": "30s",
    "severity": "critical",
    "component": "delivery",
    "summary": "Trailer throughput is below the required rate while its queue grows",
    "description": "Project Nova's trailer-critical queue is growing faster than available render throughput. Investigate Scene 87 and approve a recovery plan."
  },
  {
    "title": "Render worker capacity loss",
    "expression": "render_gpu_workers_active < 20",
    "for": "20s",
    "severity": "critical",
    "component": "workers",
    "summary": "Available GPU worker capacity has dropped",
    "description": "Fewer than 20 Project Nova GPU workers are active, putting trailer throughput at risk."
  },
  {
    "title": "Storage capacity exhausted",
    "expression": "render_storage_utilization_percent > 95",
    "for": "20s",
    "severity": "critical",
    "component": "storage",
    "summary": "Render storage is nearly full",
    "description": "Storage utilization exceeded 95%. New render outputs may fail or stall."
  },
  {
    "title": "Asset corruption detected",
    "expression": "render_asset_error_rate_percent > 5",
    "for": "20s",
    "severity": "critical",
    "component": "assets",
    "summary": "Asset validation failures are elevated",
    "description": "More than 5% of assets are failing, causing deterministic scene retries."
  },
  {
    "title": "Critical dependency unavailable",
    "expression": "render_condition_ratio{scenario=\"dependency_down\"} == 1",
    "for": "10s",
    "severity": "critical",
    "component": "dependency",
    "summary": "A required production dependency is unavailable",
    "description": "The simulator reports a dependency outage that can stop dispatch and rendering."
  }
]
